mutation CreateOneTask($data: TaskCreateInput!) {
  createOneTask(data: $data) {
    id
    name
    message
    note
    dueDate
    status {
      id
    }
    priority {
      id
    }
    files {
      id
      url
      fileId
    }
    workspace {
      id
    }
    workspaceId
  }
}
