query Tasks($workspaceId: String!) {
  tasks(where: { workspaceId: { equals: $workspaceId } }) {
    id
    name
    message
    note
    dueDate
    files {
      id
    }
    status {
      name
      color
    }
    priority {
      name
      color
    }
    workspace {
      id
    }
  }
}