mutation CreateOneTask($data: TaskCreateInput!) {
  createOneTask(data: $data) {
    id
    name
    message
    note
    dueDate
    status {
      id
    }
    files {
      id
      url
      fileId
    }
    workspace {
      id
    }
    workspaceId
  }
}
